import React from 'react'

function Title({text}) {

    return (
            <h1 className='text-center text-white text-uppercase fs-2 mb-5 text-center fw-bold'>{text}</h1>
    )
}

export default Title
